---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { Sidebar } from '../../components/layout/Sidebar';
import { Header } from '../../components/layout/Header';
import { UserManagementDashboard } from '../../components/admin/UserManagementDashboard';
import { BottomNavbar } from '../../components/layout/BottomNavbar';
---

<BaseLayout title="Manage Users | Orbit Site Admin">
    <div class="flex h-screen bg-[#F8FAFC]">
        {/* Sidebar Desktop (Sidebar A - Dark) */}
        <Sidebar activePage="users" client:load />

        {/* Main Content Area */}
        <div class="flex-1 flex flex-col min-w-0 h-screen overflow-hidden">
            <Header client:load />
            
            {/* The Dashboard acts as the main scrollable area */}
            <main class="flex-1 overflow-y-auto mt-20 p-8 md:ml-64 ml-0 pb-32">
                <UserManagementDashboard client:load />
            </main>

            {/* Bottom Navigation for Mobile */}
            <BottomNavbar variant="admin" activePage="users" client:load />
        </div>
    </div>
</BaseLayout>
